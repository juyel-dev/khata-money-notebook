# Agent Takeover

Read `../AI-CONTEXT.md` and `../ENGINEERING-INVARIANTS.md` first. Then inspect the current implementation and tests.

For cross-module changes, search all callers and semantic test assertions before editing. Re-read the complete diff before opening a PR.

The files in this directory are process memory only; they never override current code, tests, or engineering invariants.
