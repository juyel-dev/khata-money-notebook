# Agent Handoff Protocol

For every new task, the incoming agent should treat the current repository as shared memory.

```text
current code/tests
→ engineering invariants
→ focused docs
→ agent retrospectives
```

Never use another agent's retrospective as permission to change product behavior. Use it only to avoid repeating known process mistakes.
